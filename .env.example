# Copia este archivo como .env y ajusta los valores locales.

# Base de datos PostgreSQL (Prisma)
DATABASE_URL="postgresql://futbol_app:CHANGE_ME@localhost:5432/futbol_dev?schema=public"

# URL pública del sitio (metadatos y enlaces para compartir).
# En Vercel no hace falta: se usa VERCEL_PROJECT_PRODUCTION_URL.
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
